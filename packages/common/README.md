# Common Package

Shared schemas and utilities for BeliefCraft services.

## Available modules

- `common.*`: shared logging, HTTP helpers, config utilities
- `schemas.*`: shared Pydantic contracts for smart query builder

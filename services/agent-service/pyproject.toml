[project]
name = "beliefcraft-agent-service"
version = "0.1.0"
description = "Agent service for BeliefCraft."
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.27",
]

[tool.uv]
dev-dependencies = [
  "pytest>=8.0",
]

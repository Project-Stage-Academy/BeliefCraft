#tells the pre-commit tool which checks to run, and which versions.

repos:
  #basic hygiene
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: end-of-file-fixer
        exclude: ^pacts/
      - id: trailing-whitespace
        exclude: ^pacts/

  #black
  - repo: https://github.com/psf/black
    rev: 26.1.0
    hooks:
      - id: black

  #ruff
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.0
    hooks:
      - id: ruff-check
        args: ["--fix", "--exit-non-zero-on-fix"]

  #isort
  - repo: https://github.com/pycqa/isort
    rev: 7.0.0
    hooks:
      - id: isort


  #mypy, sync mcp config, and sync skills
  - repo: local
    hooks:
      - id: sync-mcp-config
        name: sync-mcp-config
        entry: uv run python scripts/sync_mcp_config.py
        language: system
        pass_filenames: false
        always_run: true
      - id: sync-skills
        name: sync-skills
        entry: uv run python scripts/sync_skills.py
        language: system
        pass_filenames: false
        always_run: true
      - id: mypy
        name: mypy
        entry: uv run mypy packages services
        language: system
        pass_filenames: false
